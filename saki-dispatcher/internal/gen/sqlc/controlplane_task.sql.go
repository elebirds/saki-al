// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: controlplane_task.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const cancelTaskByID = `-- name: CancelTaskByID :execrows
UPDATE task
SET status = 'CANCELLED'::runtimetaskstatus,
    last_error = $1,
    ended_at = COALESCE(ended_at, now()),
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
`

type CancelTaskByIDParams struct {
	LastError pgtype.Text
	TaskID    uuid.UUID
}

func (q *Queries) CancelTaskByID(ctx context.Context, arg CancelTaskByIDParams) (int64, error) {
	result, err := q.db.Exec(ctx, cancelTaskByID, arg.LastError, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteTaskByID = `-- name: DeleteTaskByID :execrows
DELETE FROM task
WHERE id = $1::uuid
`

func (q *Queries) DeleteTaskByID(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTaskByID, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failDispatchingTaskByAck = `-- name: FailDispatchingTaskByAck :execrows
UPDATE task
SET status = 'FAILED'::runtimetaskstatus,
    assigned_executor_id = NULL,
    started_at = COALESCE(started_at, now()),
    ended_at = COALESCE(ended_at, now()),
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'DISPATCHING'::runtimetaskstatus
  AND assigned_executor_id = $3
`

type FailDispatchingTaskByAckParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID pgtype.Text
}

func (q *Queries) FailDispatchingTaskByAck(ctx context.Context, arg FailDispatchingTaskByAckParams) (int64, error) {
	result, err := q.db.Exec(ctx, failDispatchingTaskByAck, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getStepIDByTaskID = `-- name: GetStepIDByTaskID :one
SELECT id
FROM step
WHERE task_id = $1::uuid
LIMIT 1
`

func (q *Queries) GetStepIDByTaskID(ctx context.Context, taskID uuid.UUID) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, getStepIDByTaskID, taskID)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}

const getTaskForUpdate = `-- name: GetTaskForUpdate :one
SELECT
  id,
  project_id,
  kind::text AS kind,
  task_type::text AS task_type,
  status::text AS status,
  plugin_id,
  input_commit_id,
  COALESCE(resolved_params, '{}'::jsonb) AS resolved_params_json,
  attempt,
  max_attempts,
  COALESCE(assigned_executor_id, '') AS assigned_executor_id,
  COALESCE(last_error, '') AS last_error
FROM task
WHERE id = $1::uuid
FOR UPDATE
`

type GetTaskForUpdateRow struct {
	ID                 uuid.UUID
	ProjectID          uuid.UUID
	Kind               string
	TaskType           string
	Status             string
	PluginID           string
	InputCommitID      *uuid.UUID
	ResolvedParamsJson []byte
	Attempt            int32
	MaxAttempts        int32
	AssignedExecutorID string
	LastError          string
}

func (q *Queries) GetTaskForUpdate(ctx context.Context, taskID uuid.UUID) (GetTaskForUpdateRow, error) {
	row := q.db.QueryRow(ctx, getTaskForUpdate, taskID)
	var i GetTaskForUpdateRow
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.Kind,
		&i.TaskType,
		&i.Status,
		&i.PluginID,
		&i.InputCommitID,
		&i.ResolvedParamsJson,
		&i.Attempt,
		&i.MaxAttempts,
		&i.AssignedExecutorID,
		&i.LastError,
	)
	return i, err
}

const getTaskIDByStepID = `-- name: GetTaskIDByStepID :one
SELECT task_id
FROM step
WHERE id = $1::uuid
`

func (q *Queries) GetTaskIDByStepID(ctx context.Context, stepID uuid.UUID) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, getTaskIDByStepID, stepID)
	var task_id uuid.UUID
	err := row.Scan(&task_id)
	return task_id, err
}

const insertStepTask = `-- name: InsertStepTask :exec
INSERT INTO task(
  created_at,
  updated_at,
  id,
  project_id,
  kind,
  task_type,
  status,
  plugin_id,
  depends_on_task_ids,
  input_commit_id,
  resolved_params,
  assigned_executor_id,
  attempt,
  max_attempts,
  started_at,
  ended_at,
  last_error
) VALUES (
  now(),
  now(),
  $1::uuid,
  $2::uuid,
  'STEP'::runtimetaskkind,
  $3::runtimetasktype,
  'PENDING'::runtimetaskstatus,
  $4,
  $5::jsonb,
  $6::uuid,
  $7::jsonb,
  NULL,
  1,
  $8,
  NULL,
  NULL,
  NULL
)
`

type InsertStepTaskParams struct {
	TaskID           uuid.UUID
	ProjectID        uuid.UUID
	TaskType         Runtimetasktype
	PluginID         string
	DependsOnTaskIds []byte
	InputCommitID    *uuid.UUID
	ResolvedParams   []byte
	MaxAttempts      int32
}

func (q *Queries) InsertStepTask(ctx context.Context, arg InsertStepTaskParams) error {
	_, err := q.db.Exec(ctx, insertStepTask,
		arg.TaskID,
		arg.ProjectID,
		arg.TaskType,
		arg.PluginID,
		arg.DependsOnTaskIds,
		arg.InputCommitID,
		arg.ResolvedParams,
		arg.MaxAttempts,
	)
	return err
}

const listInFlightTaskIDsByExecutor = `-- name: ListInFlightTaskIDsByExecutor :many
SELECT id
FROM task
WHERE assigned_executor_id = $1
  AND status IN (
    'DISPATCHING'::runtimetaskstatus,
    'SYNCING_ENV'::runtimetaskstatus,
    'PROBING_RUNTIME'::runtimetaskstatus,
    'BINDING_DEVICE'::runtimetaskstatus,
    'RUNNING'::runtimetaskstatus
  )
ORDER BY updated_at ASC
LIMIT $2
`

type ListInFlightTaskIDsByExecutorParams struct {
	ExecutorID pgtype.Text
	LimitCount int32
}

func (q *Queries) ListInFlightTaskIDsByExecutor(ctx context.Context, arg ListInFlightTaskIDsByExecutorParams) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, listInFlightTaskIDsByExecutor, arg.ExecutorID, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []uuid.UUID
	for rows.Next() {
		var id uuid.UUID
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listInFlightTaskRecoveryCandidates = `-- name: ListInFlightTaskRecoveryCandidates :many
SELECT
  t.id AS task_id,
  t.kind::text AS task_kind,
  t.status AS task_status,
  t.attempt AS attempt,
  t.max_attempts AS max_attempts,
  COALESCE(t.assigned_executor_id, '') AS assigned_executor_id,
  t.updated_at AS task_updated_at,
  COALESCE(r.is_online, FALSE) AS executor_online,
  r.last_seen_at AS executor_last_seen_at,
  COALESCE(r.current_task_id, '') AS executor_current_task_id
FROM task t
LEFT JOIN runtime_executor r ON r.executor_id = t.assigned_executor_id
WHERE t.status IN (
  'DISPATCHING'::runtimetaskstatus,
  'SYNCING_ENV'::runtimetaskstatus,
  'PROBING_RUNTIME'::runtimetaskstatus,
  'BINDING_DEVICE'::runtimetaskstatus,
  'RUNNING'::runtimetaskstatus
)
ORDER BY t.updated_at ASC
LIMIT $1
`

type ListInFlightTaskRecoveryCandidatesRow struct {
	TaskID                uuid.UUID
	TaskKind              string
	TaskStatus            Runtimetaskstatus
	Attempt               int32
	MaxAttempts           int32
	AssignedExecutorID    string
	TaskUpdatedAt         pgtype.Timestamptz
	ExecutorOnline        bool
	ExecutorLastSeenAt    pgtype.Timestamptz
	ExecutorCurrentTaskID string
}

func (q *Queries) ListInFlightTaskRecoveryCandidates(ctx context.Context, limitCount int32) ([]ListInFlightTaskRecoveryCandidatesRow, error) {
	rows, err := q.db.Query(ctx, listInFlightTaskRecoveryCandidates, limitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListInFlightTaskRecoveryCandidatesRow
	for rows.Next() {
		var i ListInFlightTaskRecoveryCandidatesRow
		if err := rows.Scan(
			&i.TaskID,
			&i.TaskKind,
			&i.TaskStatus,
			&i.Attempt,
			&i.MaxAttempts,
			&i.AssignedExecutorID,
			&i.TaskUpdatedAt,
			&i.ExecutorOnline,
			&i.ExecutorLastSeenAt,
			&i.ExecutorCurrentTaskID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markOrchestratorTaskRetrying = `-- name: MarkOrchestratorTaskRetrying :execrows
UPDATE task
SET status = 'RETRYING'::runtimetaskstatus,
    attempt = attempt + 1,
    last_error = $1,
    assigned_executor_id = NULL,
    ended_at = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'RUNNING'::runtimetaskstatus
  AND attempt < max_attempts
`

type MarkOrchestratorTaskRetryingParams struct {
	LastError pgtype.Text
	TaskID    uuid.UUID
}

func (q *Queries) MarkOrchestratorTaskRetrying(ctx context.Context, arg MarkOrchestratorTaskRetryingParams) (int64, error) {
	result, err := q.db.Exec(ctx, markOrchestratorTaskRetrying, arg.LastError, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markOrchestratorTaskRunning = `-- name: MarkOrchestratorTaskRunning :execrows
UPDATE task
SET status = 'RUNNING'::runtimetaskstatus,
    started_at = COALESCE(started_at, now()),
    last_error = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $1::uuid
  AND status = 'READY'::runtimetaskstatus
`

func (q *Queries) MarkOrchestratorTaskRunning(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, markOrchestratorTaskRunning, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTaskDispatching = `-- name: MarkTaskDispatching :execrows
UPDATE task
SET status = 'DISPATCHING'::runtimetaskstatus,
    assigned_executor_id = $1,
    last_error = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
`

type MarkTaskDispatchingParams struct {
	AssignedExecutorID pgtype.Text
	TaskID             uuid.UUID
}

func (q *Queries) MarkTaskDispatching(ctx context.Context, arg MarkTaskDispatchingParams) (int64, error) {
	result, err := q.db.Exec(ctx, markTaskDispatching, arg.AssignedExecutorID, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTaskDispatchingFromReady = `-- name: MarkTaskDispatchingFromReady :execrows
UPDATE task
SET status = 'DISPATCHING'::runtimetaskstatus,
    assigned_executor_id = $1,
    last_error = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'READY'::runtimetaskstatus
`

type MarkTaskDispatchingFromReadyParams struct {
	AssignedExecutorID pgtype.Text
	TaskID             uuid.UUID
}

func (q *Queries) MarkTaskDispatchingFromReady(ctx context.Context, arg MarkTaskDispatchingFromReadyParams) (int64, error) {
	result, err := q.db.Exec(ctx, markTaskDispatchingFromReady, arg.AssignedExecutorID, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const projectStepFromTask = `-- name: ProjectStepFromTask :execrows
UPDATE step s
SET state = t.status::text::stepstatus,
    state_version = CASE
      WHEN s.state IS DISTINCT FROM t.status::text::stepstatus THEN s.state_version + 1
      ELSE s.state_version
    END,
    attempt = t.attempt,
    max_attempts = t.max_attempts,
    assigned_executor_id = t.assigned_executor_id,
    started_at = CASE
      WHEN t.status IN (
        'RUNNING'::runtimetaskstatus,
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(s.started_at, t.started_at, now())
      ELSE s.started_at
    END,
    ended_at = CASE
      WHEN t.status IN (
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(s.ended_at, t.ended_at, now())
      ELSE s.ended_at
    END,
    last_error = t.last_error,
    updated_at = now()
FROM task t
WHERE t.id = $1::uuid
  AND s.task_id = t.id
`

func (q *Queries) ProjectStepFromTask(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, projectStepFromTask, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const promoteRetryingTaskToReady = `-- name: PromoteRetryingTaskToReady :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    last_error = NULL,
    ended_at = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $1::uuid
  AND status = 'RETRYING'::runtimetaskstatus
`

func (q *Queries) PromoteRetryingTaskToReady(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, promoteRetryingTaskToReady, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const promoteTaskToReady = `-- name: PromoteTaskToReady :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    last_error = NULL,
    ended_at = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $1::uuid
  AND status = 'PENDING'::runtimetaskstatus
`

func (q *Queries) PromoteTaskToReady(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, promoteTaskToReady, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const recoverPreRunTaskToReady = `-- name: RecoverPreRunTaskToReady :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    assigned_executor_id = NULL,
    ended_at = NULL,
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status IN (
    'DISPATCHING'::runtimetaskstatus,
    'SYNCING_ENV'::runtimetaskstatus,
    'PROBING_RUNTIME'::runtimetaskstatus,
    'BINDING_DEVICE'::runtimetaskstatus
  )
  AND (
    $3 = ''
    OR assigned_executor_id = $3
  )
`

type RecoverPreRunTaskToReadyParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID interface{}
}

func (q *Queries) RecoverPreRunTaskToReady(ctx context.Context, arg RecoverPreRunTaskToReadyParams) (int64, error) {
	result, err := q.db.Exec(ctx, recoverPreRunTaskToReady, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const recoverRunningTaskToFailed = `-- name: RecoverRunningTaskToFailed :execrows
UPDATE task
SET status = 'FAILED'::runtimetaskstatus,
    assigned_executor_id = NULL,
    started_at = COALESCE(started_at, now()),
    ended_at = COALESCE(ended_at, now()),
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'RUNNING'::runtimetaskstatus
  AND (
    $3 = ''
    OR assigned_executor_id = $3
  )
`

type RecoverRunningTaskToFailedParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID interface{}
}

func (q *Queries) RecoverRunningTaskToFailed(ctx context.Context, arg RecoverRunningTaskToFailedParams) (int64, error) {
	result, err := q.db.Exec(ctx, recoverRunningTaskToFailed, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const recoverRunningTaskToRetrying = `-- name: RecoverRunningTaskToRetrying :execrows
UPDATE task
SET status = 'RETRYING'::runtimetaskstatus,
    attempt = attempt + 1,
    assigned_executor_id = NULL,
    ended_at = NULL,
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'RUNNING'::runtimetaskstatus
  AND attempt < max_attempts
  AND (
    $3 = ''
    OR assigned_executor_id = $3
  )
`

type RecoverRunningTaskToRetryingParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID interface{}
}

func (q *Queries) RecoverRunningTaskToRetrying(ctx context.Context, arg RecoverRunningTaskToRetryingParams) (int64, error) {
	result, err := q.db.Exec(ctx, recoverRunningTaskToRetrying, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const recoverStaleDispatchingTaskToReady = `-- name: RecoverStaleDispatchingTaskToReady :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    assigned_executor_id = NULL,
    last_error = $1,
    ended_at = NULL,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status IN (
    'DISPATCHING'::runtimetaskstatus,
    'SYNCING_ENV'::runtimetaskstatus,
    'PROBING_RUNTIME'::runtimetaskstatus,
    'BINDING_DEVICE'::runtimetaskstatus
  )
`

type RecoverStaleDispatchingTaskToReadyParams struct {
	LastError pgtype.Text
	TaskID    uuid.UUID
}

func (q *Queries) RecoverStaleDispatchingTaskToReady(ctx context.Context, arg RecoverStaleDispatchingTaskToReadyParams) (int64, error) {
	result, err := q.db.Exec(ctx, recoverStaleDispatchingTaskToReady, arg.LastError, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const resetDispatchingTaskToReadyByAck = `-- name: ResetDispatchingTaskToReadyByAck :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    assigned_executor_id = NULL,
    ended_at = NULL,
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'DISPATCHING'::runtimetaskstatus
  AND assigned_executor_id = $3
`

type ResetDispatchingTaskToReadyByAckParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID pgtype.Text
}

func (q *Queries) ResetDispatchingTaskToReadyByAck(ctx context.Context, arg ResetDispatchingTaskToReadyByAckParams) (int64, error) {
	result, err := q.db.Exec(ctx, resetDispatchingTaskToReadyByAck, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const resetTaskToReadyQueueFull = `-- name: ResetTaskToReadyQueueFull :execrows
UPDATE task
SET status = 'READY'::runtimetaskstatus,
    assigned_executor_id = NULL,
    last_error = 'executor unavailable or queue full',
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $1::uuid
`

func (q *Queries) ResetTaskToReadyQueueFull(ctx context.Context, taskID uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, resetTaskToReadyQueueFull, taskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retryDispatchingTaskByAck = `-- name: RetryDispatchingTaskByAck :execrows
UPDATE task
SET status = 'RETRYING'::runtimetaskstatus,
    attempt = attempt + 1,
    assigned_executor_id = NULL,
    ended_at = NULL,
    last_error = $1,
    result_ready_at = NULL,
    updated_at = now()
WHERE id = $2::uuid
  AND status = 'DISPATCHING'::runtimetaskstatus
  AND assigned_executor_id = $3
  AND attempt < max_attempts
`

type RetryDispatchingTaskByAckParams struct {
	LastError          pgtype.Text
	TaskID             uuid.UUID
	AssignedExecutorID pgtype.Text
}

func (q *Queries) RetryDispatchingTaskByAck(ctx context.Context, arg RetryDispatchingTaskByAckParams) (int64, error) {
	result, err := q.db.Exec(ctx, retryDispatchingTaskByAck, arg.LastError, arg.TaskID, arg.AssignedExecutorID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateTaskExecutionResultGuarded = `-- name: UpdateTaskExecutionResultGuarded :execrows
UPDATE task
SET status = $1::runtimetaskstatus,
    started_at = COALESCE(started_at, now()),
    ended_at = CASE
      WHEN $1::runtimetaskstatus IN (
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(ended_at, now())
      ELSE ended_at
    END,
    last_error = $2::text,
    assigned_executor_id = CASE
      WHEN $1::runtimetaskstatus IN (
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus,
        'RETRYING'::runtimetaskstatus
      ) THEN NULL
      ELSE assigned_executor_id
    END,
    result_ready_at = CASE
      WHEN $1::runtimetaskstatus = 'SUCCEEDED'::runtimetaskstatus THEN COALESCE(result_ready_at, now())
      ELSE NULL
    END,
    updated_at = now()
WHERE id = $3::uuid
  AND status = $4::runtimetaskstatus
`

type UpdateTaskExecutionResultGuardedParams struct {
	Status     Runtimetaskstatus
	LastError  pgtype.Text
	TaskID     uuid.UUID
	FromStatus Runtimetaskstatus
}

func (q *Queries) UpdateTaskExecutionResultGuarded(ctx context.Context, arg UpdateTaskExecutionResultGuardedParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateTaskExecutionResultGuarded,
		arg.Status,
		arg.LastError,
		arg.TaskID,
		arg.FromStatus,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateTaskResult = `-- name: UpdateTaskResult :execrows
UPDATE task
SET status = $1::runtimetaskstatus,
    resolved_params = $2::jsonb,
    started_at = COALESCE(started_at, now()),
    ended_at = CASE
      WHEN $1::runtimetaskstatus IN (
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(ended_at, now())
      ELSE ended_at
    END,
    result_ready_at = CASE
      WHEN $1::runtimetaskstatus = 'SUCCEEDED'::runtimetaskstatus THEN COALESCE(result_ready_at, now())
      ELSE NULL
    END,
    last_error = $3::text,
    updated_at = now()
WHERE id = $4::uuid
`

type UpdateTaskResultParams struct {
	Status         Runtimetaskstatus
	ResolvedParams []byte
	LastError      pgtype.Text
	TaskID         uuid.UUID
}

func (q *Queries) UpdateTaskResult(ctx context.Context, arg UpdateTaskResultParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateTaskResult,
		arg.Status,
		arg.ResolvedParams,
		arg.LastError,
		arg.TaskID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateTaskStatusLifecycle = `-- name: UpdateTaskStatusLifecycle :execrows
UPDATE task
SET status = $1::runtimetaskstatus,
    started_at = CASE
      WHEN $1::runtimetaskstatus IN (
        'RUNNING'::runtimetaskstatus,
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(started_at, now())
      ELSE started_at
    END,
    ended_at = CASE
      WHEN $1::runtimetaskstatus IN (
        'SUCCEEDED'::runtimetaskstatus,
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN COALESCE(ended_at, now())
      ELSE ended_at
    END,
    result_ready_at = CASE
      WHEN $1::runtimetaskstatus IN (
        'PENDING'::runtimetaskstatus,
        'READY'::runtimetaskstatus,
        'DISPATCHING'::runtimetaskstatus,
        'SYNCING_ENV'::runtimetaskstatus,
        'PROBING_RUNTIME'::runtimetaskstatus,
        'BINDING_DEVICE'::runtimetaskstatus,
        'RUNNING'::runtimetaskstatus,
        'RETRYING'::runtimetaskstatus
      ) THEN NULL
      WHEN $1::runtimetaskstatus IN (
        'FAILED'::runtimetaskstatus,
        'CANCELLED'::runtimetaskstatus,
        'SKIPPED'::runtimetaskstatus
      ) THEN NULL
      ELSE result_ready_at
    END,
    last_error = $2::text,
    updated_at = now()
WHERE id = $3::uuid
`

type UpdateTaskStatusLifecycleParams struct {
	Status    Runtimetaskstatus
	LastError pgtype.Text
	TaskID    uuid.UUID
}

func (q *Queries) UpdateTaskStatusLifecycle(ctx context.Context, arg UpdateTaskStatusLifecycleParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateTaskStatusLifecycle, arg.Status, arg.LastError, arg.TaskID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
