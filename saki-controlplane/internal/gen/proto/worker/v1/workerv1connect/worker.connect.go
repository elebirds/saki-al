// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: worker/v1/worker.proto

package workerv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/elebirds/saki/saki-controlplane/internal/gen/proto/worker/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkerName is the fully-qualified name of the Worker service.
	WorkerName = "saki.worker.v1.Worker"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkerRunProcedure is the fully-qualified name of the Worker's Run RPC.
	WorkerRunProcedure = "/saki.worker.v1.Worker/Run"
)

// WorkerClient is a client for the saki.worker.v1.Worker service.
type WorkerClient interface {
	Run(context.Context, *connect.Request[v1.RunRequest]) (*connect.Response[v1.RunResponse], error)
}

// NewWorkerClient constructs a client for the saki.worker.v1.Worker service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkerClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkerClient {
	baseURL = strings.TrimRight(baseURL, "/")
	workerMethods := v1.File_worker_v1_worker_proto.Services().ByName("Worker").Methods()
	return &workerClient{
		run: connect.NewClient[v1.RunRequest, v1.RunResponse](
			httpClient,
			baseURL+WorkerRunProcedure,
			connect.WithSchema(workerMethods.ByName("Run")),
			connect.WithClientOptions(opts...),
		),
	}
}

// workerClient implements WorkerClient.
type workerClient struct {
	run *connect.Client[v1.RunRequest, v1.RunResponse]
}

// Run calls saki.worker.v1.Worker.Run.
func (c *workerClient) Run(ctx context.Context, req *connect.Request[v1.RunRequest]) (*connect.Response[v1.RunResponse], error) {
	return c.run.CallUnary(ctx, req)
}

// WorkerHandler is an implementation of the saki.worker.v1.Worker service.
type WorkerHandler interface {
	Run(context.Context, *connect.Request[v1.RunRequest]) (*connect.Response[v1.RunResponse], error)
}

// NewWorkerHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkerHandler(svc WorkerHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workerMethods := v1.File_worker_v1_worker_proto.Services().ByName("Worker").Methods()
	workerRunHandler := connect.NewUnaryHandler(
		WorkerRunProcedure,
		svc.Run,
		connect.WithSchema(workerMethods.ByName("Run")),
		connect.WithHandlerOptions(opts...),
	)
	return "/saki.worker.v1.Worker/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkerRunProcedure:
			workerRunHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkerHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkerHandler struct{}

func (UnimplementedWorkerHandler) Run(context.Context, *connect.Request[v1.RunRequest]) (*connect.Response[v1.RunResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("saki.worker.v1.Worker.Run is not implemented"))
}
