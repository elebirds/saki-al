// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: preview.sql

package sqlcdb

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getImportPreviewManifest = `-- name: GetImportPreviewManifest :one
select token, mode, project_id, dataset_id, upload_session_id, manifest, params_hash, expires_at, created_at
from import_preview_manifest
where token = $1
`

func (q *Queries) GetImportPreviewManifest(ctx context.Context, token string) (ImportPreviewManifest, error) {
	row := q.db.QueryRow(ctx, getImportPreviewManifest, token)
	var i ImportPreviewManifest
	err := row.Scan(
		&i.Token,
		&i.Mode,
		&i.ProjectID,
		&i.DatasetID,
		&i.UploadSessionID,
		&i.Manifest,
		&i.ParamsHash,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const putImportPreviewManifest = `-- name: PutImportPreviewManifest :one
insert into import_preview_manifest (
    token,
    mode,
    project_id,
    dataset_id,
    upload_session_id,
    manifest,
    params_hash,
    expires_at
)
values (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8
)
on conflict (token) do update
set mode = excluded.mode,
    project_id = excluded.project_id,
    dataset_id = excluded.dataset_id,
    upload_session_id = excluded.upload_session_id,
    manifest = excluded.manifest,
    params_hash = excluded.params_hash,
    expires_at = excluded.expires_at
returning token, mode, project_id, dataset_id, upload_session_id, manifest, params_hash, expires_at, created_at
`

type PutImportPreviewManifestParams struct {
	Token           string             `json:"token"`
	Mode            string             `json:"mode"`
	ProjectID       uuid.UUID          `json:"project_id"`
	DatasetID       uuid.UUID          `json:"dataset_id"`
	UploadSessionID uuid.UUID          `json:"upload_session_id"`
	Manifest        []byte             `json:"manifest"`
	ParamsHash      string             `json:"params_hash"`
	ExpiresAt       pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) PutImportPreviewManifest(ctx context.Context, arg PutImportPreviewManifestParams) (ImportPreviewManifest, error) {
	row := q.db.QueryRow(ctx, putImportPreviewManifest,
		arg.Token,
		arg.Mode,
		arg.ProjectID,
		arg.DatasetID,
		arg.UploadSessionID,
		arg.Manifest,
		arg.ParamsHash,
		arg.ExpiresAt,
	)
	var i ImportPreviewManifest
	err := row.Scan(
		&i.Token,
		&i.Mode,
		&i.ProjectID,
		&i.DatasetID,
		&i.UploadSessionID,
		&i.Manifest,
		&i.ParamsHash,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}
