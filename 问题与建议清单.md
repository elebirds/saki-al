# 数据模型重构 - 问题与建议清单

> **日期**: 2026年1月21日  
> **审查人**: GitHub Copilot  
> **状态**: 待处理

---

## 🚨 关键问题（需要立即修复）

### 问题 1: Branch.name 字段类型错误

**位置**: `models/l2/branch.py:24`

**当前实现**:
```python
name: uuid.UUID = Field(index=True, max_length=100, ...)
```

**问题描述**:
- 分支名应该是字符串（如 "master", "al-iter-1"），而非 UUID
- UUID 类型不支持 max_length 参数
- 这会导致创建分支时类型错误

**建议修复**:
```python
name: str = Field(index=True, max_length=100, 
                  description="Branch name (e.g., 'master', 'al-iter-1').")
```

**影响范围**: 所有分支创建和查询操作

**优先级**: 🔴 **P0 - 阻塞**

---

## ⚠️ 设计问题（建议优化）

### 问题 2: Annotation 的 sync_id 命名歧义

**位置**: `models/l2/annotation.py:37`

**当前实现**:
```python
sync_id: uuid.UUID = Field(
    index=True,
    nullable=False,
    description="Logical UUID for synchronizing annotations across views."
)
```

**问题描述**:
- 在之前的设计文档中，我们称之为 `uuid`
- 现在改为 `sync_id` 更清晰，但需要更新相关文档
- 前端可能期望字段名为 `uuid`

**建议**:
- 保持 `sync_id` 命名（更语义化）
- 更新所有相关文档
- 在 API 层提供 `uuid` 别名以保持向后兼容

**优先级**: 🟡 **P2 - 中等**

---

### 问题 3: Asset 去重机制的完整性

**位置**: `models/l1/asset.py`

**当前实现**:
```python
hash: str = Field(max_length=64, index=True, unique=True, ...)
```

**潜在问题**:
1. **哈希冲突**: 虽然 SHA256 冲突概率极低，但理论上存在
2. **删除策略**: 当最后一个引用的 Sample 被删除时，Asset 是否应该删除？
3. **引用计数**: 没有追踪有多少 Sample 引用了该 Asset

**建议方案**:

#### 方案 A: 添加引用计数（简单）
```python
class Asset:
    reference_count: int = Field(default=0, description="Number of samples referencing this asset")
```

**优点**: 实现简单  
**缺点**: 需要应用层维护一致性

#### 方案 B: 定期垃圾回收（推荐）
```python
# 定期任务：清理无引用的 Asset
SELECT a.id 
FROM asset a 
LEFT JOIN sample s ON JSON_CONTAINS(s.asset_group, CONCAT('"', a.hash, '"'))
WHERE s.id IS NULL
AND a.created_at < NOW() - INTERVAL 7 DAY;
```

**优点**: 解耦，无需实时维护  
**缺点**: 有延迟

**优先级**: 🟢 **P3 - 低（可后续优化）**

---

### 问题 4: CommitAnnotationMap 的数据一致性

**位置**: `models/l2/camap.py`

**当前实现**:
```python
project_id: uuid.UUID = Field(foreign_key="project.id", index=True)
```

**问题描述**:
- `project_id` 是冗余字段（可以通过 commit_id → commit.project_id 获取）
- 但如果 commit.project_id 和 map.project_id 不一致，会导致数据混乱

**建议**:
1. **保留冗余字段**（性能优化）
2. **添加触发器或约束**确保一致性:
   ```sql
   CREATE TRIGGER check_commit_annotation_map_project
   BEFORE INSERT ON commit_annotation_map
   FOR EACH ROW
   BEGIN
       DECLARE commit_project_id UUID;
       SELECT project_id INTO commit_project_id FROM commit WHERE id = NEW.commit_id;
       IF NEW.project_id != commit_project_id THEN
           SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'project_id mismatch';
       END IF;
   END;
   ```
3. **或者移除 project_id**，在查询时 JOIN:
   ```python
   # 移除冗余字段
   class CommitAnnotationMap:
       # project_id: uuid.UUID  # 移除
   ```

**权衡**:
- **保留**: 查询快，但有一致性风险
- **移除**: 查询慢（需要 JOIN），但数据绝对一致

**建议**: 保留，但在应用层严格校验

**优先级**: 🟡 **P2 - 中等**

---

### 问题 5: Annotation 修改历史查询性能

**位置**: `models/l2/annotation.py`

**当前实现**:
```python
parent_id: uuid.UUID | None = Field(
    default=None,
    foreign_key="annotation.id",
    index=True,  # ✅ 已有索引
    ...
)
```

**潜在问题**:
- 查询完整修改历史需要递归 `parent_id` 链
- 如果修改次数很多（如 10+ 次），性能可能下降

**建议优化**:

#### 方案 A: 添加 root_id 字段
```python
class Annotation:
    parent_id: uuid.UUID | None
    root_id: uuid.UUID = Field(description="ID of the first annotation in the chain")
```

**优点**: 快速查询同一标注的所有版本  
**缺点**: 需要额外存储

#### 方案 B: 使用 PostgreSQL 递归 CTE（推荐）
```sql
WITH RECURSIVE history AS (
  SELECT id, parent_id, data, created_at, 0 as depth
  FROM annotation
  WHERE id = 'target_id'
  
  UNION ALL
  
  SELECT a.id, a.parent_id, a.data, a.created_at, h.depth + 1
  FROM annotation a
  JOIN history h ON a.id = h.parent_id
  WHERE h.depth < 100  -- 防止无限递归
)
SELECT * FROM history ORDER BY depth;
```

**优点**: 不需要额外字段，SQL 标准支持  
**缺点**: 深度过大时性能下降（但通常不会超过 10 层）

**优先级**: 🟢 **P3 - 低（可监控后优化）**

---

## 💡 设计建议（增强功能）

### 建议 1: 添加 Annotation 的软删除支持

**背景**: 当前 Annotation 不可变，但用户可能需要"删除"标注。

**建议实现**:
```python
class Annotation:
    is_deleted: bool = Field(default=False, index=True)
    deleted_at: datetime | None = None
    deleted_by: uuid.UUID | None = None
```

**查询时过滤**:
```python
annotations = session.query(Annotation).filter(
    Annotation.is_deleted == False
).all()
```

**优先级**: 🟡 **P2 - 中等**

---

### 建议 2: 为 Commit 添加 tag 支持

**背景**: Git 支持 tag，用于标记重要版本（如 "v1.0-release"）。

**建议实现**:
```python
class CommitTag(SQLModel, table=True):
    __tablename__ = "commit_tag"
    
    commit_id: uuid.UUID = Field(primary_key=True, foreign_key="commit.id")
    tag_name: str = Field(max_length=100, unique=True)
    description: str | None
    created_at: datetime
    creator_id: uuid.UUID | None
```

**用途**:
- 标记重要里程碑（如 "第一轮标注完成"）
- 快速回溯到特定版本

**优先级**: 🟢 **P3 - 低（增强功能）**

---

### 建议 3: 为 Branch 添加合并支持

**背景**: Git 支持分支合并，但当前设计没有合并机制。

**挑战**:
- 标注冲突如何解决？（同一 Sample 在两个分支有不同标注）
- 合并策略：保留哪个？都保留？提示用户选择？

**建议实现**:
```python
class BranchMerge(SQLModel, table=True):
    __tablename__ = "branch_merge"
    
    source_branch_id: uuid.UUID = Field(foreign_key="branch.id")
    target_branch_id: uuid.UUID = Field(foreign_key="branch.id")
    merge_commit_id: uuid.UUID = Field(foreign_key="commit.id")
    
    conflicts_resolved: Dict[str, Any]  # 记录冲突解决方案
    merged_at: datetime
    merged_by: uuid.UUID
```

**合并策略**:
1. **最新优先**: 取时间戳更晚的标注
2. **人工优先**: 人工标注优先于模型预测
3. **手动解决**: 提示用户选择

**优先级**: 🟢 **P3 - 低（高级功能）**

---

## 📋 数据库迁移注意事项

### 迁移风险评估

| 操作 | 风险等级 | 风险描述 | 缓解措施 |
|------|---------|---------|---------|
| 创建新表 | 🟢 低 | 不影响现有数据 | 正常执行 |
| Sample 添加 asset_group | 🟡 中 | 需要迁移 URL 到 Asset | 先创建 Asset，再更新 Sample |
| Sample 删除 status | 🔴 高 | 状态信息丢失 | 迁移前导出状态到 Commit.stats |
| Annotation 添加 sync_id | 🟡 中 | 需要生成 UUID | 为现有记录生成 UUID |
| Annotation 添加 parent_id | 🟢 低 | 默认 NULL | 直接添加列 |

### 迁移步骤建议

#### 第 1 步: 备份（必须）
```bash
pg_dump saki_db > backup_$(date +%Y%m%d_%H%M%S).sql
```

#### 第 2 步: 创建新表（安全）
```sql
CREATE TABLE asset (...);
CREATE TABLE commit (...);
CREATE TABLE commit_annotation_map (...);
CREATE TABLE branch (...);
CREATE TABLE loop (...);
CREATE TABLE job (...);
```

#### 第 3 步: 迁移 Asset（需注意）
```python
# 伪代码
for sample in old_samples:
    if sample.url:
        # 下载文件，计算哈希
        file_content = download_from_minio(sample.url)
        file_hash = hashlib.sha256(file_content).hexdigest()
        
        # 检查是否已存在
        asset = Asset.get_by_hash(file_hash)
        if not asset:
            asset = Asset(
                hash=file_hash,
                path=extract_path(sample.url),
                ...
            )
            session.add(asset)
        
        # 更新 Sample
        sample.asset_group = {"main": asset.hash}
```

#### 第 4 步: 初始化版本控制（关键）
```python
for project in projects:
    # 创建初始 Commit
    init_commit = Commit(
        project_id=project.id,
        message="Initial commit (migrated)",
        author_type=AuthorType.SYSTEM
    )
    
    # 创建 master Branch
    master = Branch(
        name="master",
        project_id=project.id,
        head_commit_id=init_commit.id,
        is_protected=True
    )
    
    # 迁移 Annotation → CommitAnnotationMap
    for sample in project.samples:
        latest_annotation = sample.annotations[-1] if sample.annotations else None
        if latest_annotation:
            CommitAnnotationMap(
                commit_id=init_commit.id,
                sample_id=sample.id,
                annotation_id=latest_annotation.id,
                project_id=project.id
            )
```

#### 第 5 步: 验证（必须）
```python
# 验证脚本
def validate_migration():
    checks = [
        ("每个 Project 都有 master Branch", check_master_branches),
        ("CommitAnnotationMap 数量正确", check_map_count),
        ("Asset 去重成功", check_asset_dedup),
        ("Annotation sync_id 已生成", check_sync_ids),
    ]
    
    for name, check_func in checks:
        result = check_func()
        print(f"{'✅' if result else '❌'} {name}")
```

---

## 🔍 测试建议

### 单元测试

```python
# tests/models/test_annotation_immutability.py
def test_annotation_immutable():
    """测试标注不可变性"""
    annotation = Annotation(...)
    session.add(annotation)
    session.commit()
    
    # 尝试修改应该创建新记录
    new_annotation = Annotation(
        sync_id=annotation.sync_id,
        parent_id=annotation.id,
        data=modified_data
    )
    session.add(new_annotation)
    
    # 验证旧记录未变
    assert annotation.data == original_data
    assert new_annotation.parent_id == annotation.id

# tests/models/test_asset_dedup.py
def test_asset_deduplication():
    """测试文件去重"""
    # 相同内容的文件
    file_content = b"test content"
    hash1 = hashlib.sha256(file_content).hexdigest()
    
    asset1 = Asset(hash=hash1, ...)
    session.add(asset1)
    session.commit()
    
    # 尝试创建相同哈希的 Asset
    asset2 = Asset(hash=hash1, ...)
    with pytest.raises(IntegrityError):
        session.add(asset2)
        session.commit()
```

### 性能测试

```python
# tests/performance/test_commit_annotation_map.py
def test_version_switch_performance():
    """测试版本切换性能"""
    # 准备 20,000 个样本
    samples = [Sample(...) for _ in range(20000)]
    
    # 创建 Commit 和映射
    commit = Commit(...)
    maps = [
        CommitAnnotationMap(
            commit_id=commit.id,
            sample_id=s.id,
            annotation_id=...
        )
        for s in samples
    ]
    session.bulk_save_objects(maps)
    
    # 测试查询性能
    start = time.time()
    annotations = (
        session.query(Annotation)
        .join(CommitAnnotationMap)
        .filter(CommitAnnotationMap.commit_id == commit.id)
        .all()
    )
    duration = time.time() - start
    
    # 断言：< 100ms
    assert duration < 0.1, f"Too slow: {duration}s"
```

### 集成测试

```python
# tests/integration/test_active_learning_flow.py
def test_complete_al_iteration():
    """测试完整的主动学习迭代流程"""
    # 1. 创建项目和初始数据
    project = create_project_with_samples(1000)
    
    # 2. 人工标注
    annotations = manually_annotate(project, count=100)
    commit1 = create_commit(annotations)
    
    # 3. 训练模型
    job = create_training_job(
        project=project,
        source_commit_id=commit1.id
    )
    assert job.status == JobStatus.SUCCESS
    
    # 4. 模型预测
    predictions = model_predict(job, unlabeled_samples)
    commit2 = create_commit(predictions, author_type=AuthorType.MODEL)
    
    # 5. 验证版本切换
    branch = project.branches[0]
    branch.head_commit_id = commit2.id
    
    loaded_annotations = load_annotations(project, branch)
    assert len(loaded_annotations) == 1000
```

---

## 📊 监控指标建议

### 性能指标

| 指标 | 目标 | 告警阈值 |
|------|------|---------|
| `commit_creation_time` | < 1s | > 2s |
| `branch_switch_time` | < 100ms | > 500ms |
| `annotation_map_query_time` | < 50ms | > 200ms |
| `asset_dedup_rate` | > 30% | < 10% |

### 数据指标

| 指标 | 监控目的 |
|------|---------|
| `commit_count_per_project` | 版本控制活跃度 |
| `annotation_modification_depth` | 标注修改次数分布 |
| `asset_orphan_count` | 孤立 Asset 数量 |
| `commit_annotation_map_size` | 映射表大小增长 |

---

## ✅ 行动项

### 本周（紧急）

- [ ] 修复 Branch.name 类型错误
- [ ] 完成数据库迁移脚本
- [ ] 编写单元测试

### 下周（重要）

- [ ] 性能测试（20k 样本）
- [ ] API 层适配
- [ ] 前端集成准备

### 本月（常规）

- [ ] 垃圾回收机制
- [ ] 监控 Dashboard
- [ ] 完整文档

---

**审查人**: GitHub Copilot  
**下次审查**: 数据库迁移完成后
