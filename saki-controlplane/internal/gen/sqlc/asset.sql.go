// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: asset.sql

package sqlcdb

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createPendingAsset = `-- name: CreatePendingAsset :one
insert into asset (
    kind,
    status,
    storage_backend,
    bucket,
    object_key,
    content_type,
    metadata,
    created_by
) values (
    $1,
    'pending_upload',
    $2,
    $3,
    $4,
    $5,
    $6,
    $7::uuid
)
returning id, kind, status, storage_backend, bucket, object_key, content_type, size_bytes, sha256_hex, metadata, created_by, ready_at, orphaned_at, created_at, updated_at
`

type CreatePendingAssetParams struct {
	Kind           AssetKind           `json:"kind"`
	StorageBackend AssetStorageBackend `json:"storage_backend"`
	Bucket         string              `json:"bucket"`
	ObjectKey      string              `json:"object_key"`
	ContentType    string              `json:"content_type"`
	Metadata       []byte              `json:"metadata"`
	CreatedBy      pgtype.UUID         `json:"created_by"`
}

func (q *Queries) CreatePendingAsset(ctx context.Context, arg CreatePendingAssetParams) (Asset, error) {
	row := q.db.QueryRow(ctx, createPendingAsset,
		arg.Kind,
		arg.StorageBackend,
		arg.Bucket,
		arg.ObjectKey,
		arg.ContentType,
		arg.Metadata,
		arg.CreatedBy,
	)
	var i Asset
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Status,
		&i.StorageBackend,
		&i.Bucket,
		&i.ObjectKey,
		&i.ContentType,
		&i.SizeBytes,
		&i.Sha256Hex,
		&i.Metadata,
		&i.CreatedBy,
		&i.ReadyAt,
		&i.OrphanedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteAsset = `-- name: DeleteAsset :execrows
delete from asset
where id = $1
`

func (q *Queries) DeleteAsset(ctx context.Context, id uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteAsset, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getAsset = `-- name: GetAsset :one
select id, kind, status, storage_backend, bucket, object_key, content_type, size_bytes, sha256_hex, metadata, created_by, ready_at, orphaned_at, created_at, updated_at
from asset
where id = $1
`

func (q *Queries) GetAsset(ctx context.Context, id uuid.UUID) (Asset, error) {
	row := q.db.QueryRow(ctx, getAsset, id)
	var i Asset
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Status,
		&i.StorageBackend,
		&i.Bucket,
		&i.ObjectKey,
		&i.ContentType,
		&i.SizeBytes,
		&i.Sha256Hex,
		&i.Metadata,
		&i.CreatedBy,
		&i.ReadyAt,
		&i.OrphanedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAssetByStorageLocation = `-- name: GetAssetByStorageLocation :one
select id, kind, status, storage_backend, bucket, object_key, content_type, size_bytes, sha256_hex, metadata, created_by, ready_at, orphaned_at, created_at, updated_at
from asset
where bucket = $1
  and object_key = $2
`

type GetAssetByStorageLocationParams struct {
	Bucket    string `json:"bucket"`
	ObjectKey string `json:"object_key"`
}

func (q *Queries) GetAssetByStorageLocation(ctx context.Context, arg GetAssetByStorageLocationParams) (Asset, error) {
	row := q.db.QueryRow(ctx, getAssetByStorageLocation, arg.Bucket, arg.ObjectKey)
	var i Asset
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Status,
		&i.StorageBackend,
		&i.Bucket,
		&i.ObjectKey,
		&i.ContentType,
		&i.SizeBytes,
		&i.Sha256Hex,
		&i.Metadata,
		&i.CreatedBy,
		&i.ReadyAt,
		&i.OrphanedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getReadyOrphanedAssetForUpdate = `-- name: GetReadyOrphanedAssetForUpdate :one
select a.id, a.kind, a.status, a.storage_backend, a.bucket, a.object_key, a.content_type, a.size_bytes, a.sha256_hex, a.metadata, a.created_by, a.ready_at, a.orphaned_at, a.created_at, a.updated_at
from asset as a
where a.id = $1
  and a.status = 'ready'
  and a.orphaned_at is not null
  and a.orphaned_at <= $2
  and not exists (
      select 1
      from asset_reference as r
      where r.asset_id = a.id
        and r.deleted_at is null
  )
for update
`

type GetReadyOrphanedAssetForUpdateParams struct {
	ID     uuid.UUID          `json:"id"`
	Cutoff pgtype.Timestamptz `json:"cutoff"`
}

func (q *Queries) GetReadyOrphanedAssetForUpdate(ctx context.Context, arg GetReadyOrphanedAssetForUpdateParams) (Asset, error) {
	row := q.db.QueryRow(ctx, getReadyOrphanedAssetForUpdate, arg.ID, arg.Cutoff)
	var i Asset
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Status,
		&i.StorageBackend,
		&i.Bucket,
		&i.ObjectKey,
		&i.ContentType,
		&i.SizeBytes,
		&i.Sha256Hex,
		&i.Metadata,
		&i.CreatedBy,
		&i.ReadyAt,
		&i.OrphanedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listReadyOrphanedAssets = `-- name: ListReadyOrphanedAssets :many
select a.id, a.kind, a.status, a.storage_backend, a.bucket, a.object_key, a.content_type, a.size_bytes, a.sha256_hex, a.metadata, a.created_by, a.ready_at, a.orphaned_at, a.created_at, a.updated_at
from asset as a
where a.status = 'ready'
  and a.orphaned_at is not null
  and a.orphaned_at <= $1
  and not exists (
      select 1
      from asset_reference as r
      where r.asset_id = a.id
        and r.deleted_at is null
  )
order by a.orphaned_at, a.id
`

func (q *Queries) ListReadyOrphanedAssets(ctx context.Context, cutoff pgtype.Timestamptz) ([]Asset, error) {
	rows, err := q.db.Query(ctx, listReadyOrphanedAssets, cutoff)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Asset
	for rows.Next() {
		var i Asset
		if err := rows.Scan(
			&i.ID,
			&i.Kind,
			&i.Status,
			&i.StorageBackend,
			&i.Bucket,
			&i.ObjectKey,
			&i.ContentType,
			&i.SizeBytes,
			&i.Sha256Hex,
			&i.Metadata,
			&i.CreatedBy,
			&i.ReadyAt,
			&i.OrphanedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStalePendingAssets = `-- name: ListStalePendingAssets :many
select a.id, a.kind, a.status, a.storage_backend, a.bucket, a.object_key, a.content_type, a.size_bytes, a.sha256_hex, a.metadata, a.created_by, a.ready_at, a.orphaned_at, a.created_at, a.updated_at
from asset as a
where a.status = 'pending_upload'
  and a.created_at <= $1
  and not exists (
      select 1
      from asset_upload_intent as i
      where i.asset_id = a.id
        and i.state = 'initiated'
        and i.expires_at > $2
  )
  and not exists (
      select 1
      from asset_reference as r
      where r.asset_id = a.id
        and r.deleted_at is null
  )
order by a.created_at, a.id
`

type ListStalePendingAssetsParams struct {
	Cutoff pgtype.Timestamptz `json:"cutoff"`
	Now    pgtype.Timestamptz `json:"now"`
}

func (q *Queries) ListStalePendingAssets(ctx context.Context, arg ListStalePendingAssetsParams) ([]Asset, error) {
	rows, err := q.db.Query(ctx, listStalePendingAssets, arg.Cutoff, arg.Now)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Asset
	for rows.Next() {
		var i Asset
		if err := rows.Scan(
			&i.ID,
			&i.Kind,
			&i.Status,
			&i.StorageBackend,
			&i.Bucket,
			&i.ObjectKey,
			&i.ContentType,
			&i.SizeBytes,
			&i.Sha256Hex,
			&i.Metadata,
			&i.CreatedBy,
			&i.ReadyAt,
			&i.OrphanedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markAssetReady = `-- name: MarkAssetReady :one
update asset
set status = 'ready',
    size_bytes = $1,
    sha256_hex = $2::text,
    content_type = $3,
    ready_at = coalesce(ready_at, now()),
    orphaned_at = null,
    updated_at = now()
where id = $4
  and status = 'pending_upload'
returning id, kind, status, storage_backend, bucket, object_key, content_type, size_bytes, sha256_hex, metadata, created_by, ready_at, orphaned_at, created_at, updated_at
`

type MarkAssetReadyParams struct {
	SizeBytes   int64       `json:"size_bytes"`
	Sha256Hex   pgtype.Text `json:"sha256_hex"`
	ContentType string      `json:"content_type"`
	ID          uuid.UUID   `json:"id"`
}

func (q *Queries) MarkAssetReady(ctx context.Context, arg MarkAssetReadyParams) (Asset, error) {
	row := q.db.QueryRow(ctx, markAssetReady,
		arg.SizeBytes,
		arg.Sha256Hex,
		arg.ContentType,
		arg.ID,
	)
	var i Asset
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Status,
		&i.StorageBackend,
		&i.Bucket,
		&i.ObjectKey,
		&i.ContentType,
		&i.SizeBytes,
		&i.Sha256Hex,
		&i.Metadata,
		&i.CreatedBy,
		&i.ReadyAt,
		&i.OrphanedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
