// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: executor.sql

package sqlcdb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const heartbeatRuntimeExecutor = `-- name: HeartbeatRuntimeExecutor :one
update runtime_executor
set last_seen_at = $1,
    status = 'online',
    updated_at = now()
where id = $2
returning id, version, capabilities, status, last_seen_at, created_at, updated_at
`

type HeartbeatRuntimeExecutorParams struct {
	LastSeenAt pgtype.Timestamptz `json:"last_seen_at"`
	ID         string             `json:"id"`
}

func (q *Queries) HeartbeatRuntimeExecutor(ctx context.Context, arg HeartbeatRuntimeExecutorParams) (RuntimeExecutor, error) {
	row := q.db.QueryRow(ctx, heartbeatRuntimeExecutor, arg.LastSeenAt, arg.ID)
	var i RuntimeExecutor
	err := row.Scan(
		&i.ID,
		&i.Version,
		&i.Capabilities,
		&i.Status,
		&i.LastSeenAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listRuntimeExecutors = `-- name: ListRuntimeExecutors :many
select id, version, capabilities, status, last_seen_at, created_at, updated_at
from runtime_executor
order by id
`

func (q *Queries) ListRuntimeExecutors(ctx context.Context) ([]RuntimeExecutor, error) {
	rows, err := q.db.Query(ctx, listRuntimeExecutors)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []RuntimeExecutor
	for rows.Next() {
		var i RuntimeExecutor
		if err := rows.Scan(
			&i.ID,
			&i.Version,
			&i.Capabilities,
			&i.Status,
			&i.LastSeenAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const registerRuntimeExecutor = `-- name: RegisterRuntimeExecutor :one
insert into runtime_executor (id, version, capabilities, status, last_seen_at)
values (
    $1,
    $2,
    $3::text[],
    'online',
    $4
)
on conflict (id) do update
set version = excluded.version,
    capabilities = excluded.capabilities,
    status = 'online',
    last_seen_at = excluded.last_seen_at,
    updated_at = now()
returning id, version, capabilities, status, last_seen_at, created_at, updated_at
`

type RegisterRuntimeExecutorParams struct {
	ID           string             `json:"id"`
	Version      string             `json:"version"`
	Capabilities []string           `json:"capabilities"`
	LastSeenAt   pgtype.Timestamptz `json:"last_seen_at"`
}

func (q *Queries) RegisterRuntimeExecutor(ctx context.Context, arg RegisterRuntimeExecutorParams) (RuntimeExecutor, error) {
	row := q.db.QueryRow(ctx, registerRuntimeExecutor,
		arg.ID,
		arg.Version,
		arg.Capabilities,
		arg.LastSeenAt,
	)
	var i RuntimeExecutor
	err := row.Scan(
		&i.ID,
		&i.Version,
		&i.Capabilities,
		&i.Status,
		&i.LastSeenAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
