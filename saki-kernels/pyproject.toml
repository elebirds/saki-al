[project]
name = "saki-kernels"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3.0"]

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"
